extends Sprite


# Declare member variables here. Examples:
# var a = 2
# var b = "text"

func _on_Actionbox_area_interacted():
	#check if player doing it.
	#the player has travelled from a wherever to a dungeon.
	#get the world to save itself
	#set world environment to dungeon
	#get the world to load itself or generate itself.
	#spawn the player.
	pass

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
